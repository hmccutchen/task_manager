<div class="task-list">
  <div class="container">
    <%= simple_form_for @task do |t|%>
    <p>Title<%= t.input :title %></p>
    <p>Details<%= t.input :details %></p>
    <%= t.button :submit, class: 'btn btn-primary' %>
    <% end %>
  </div>
</div>
